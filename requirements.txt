# Core bot
discord.py>=2.4.0
openai>=1.58.1
aiohttp>=3.9.0
python-dotenv>=1.0.1
pytz>=2023.3
numpy>=2.0.0
Pillow>=10.4.0
colorama>=0.4.6
colorlog>=6.9.0

# Web control panel
fastapi>=0.115.6
uvicorn[standard]>=0.30.6
jinja2>=3.1.4
starlette>=0.41.0

# Search and content extraction
# ddgs is the renamed duckduckgo-search package (the old name still
# exists on PyPI but no longer ships under the `ddgs` import path).
# All five cogs that hit DuckDuckGo do `from ddgs import DDGS`.
ddgs>=9.0.0
trafilatura>=2.0.0
beautifulsoup4>=4.12.3
html2text>=2024.2.26
# lxml >=5.2 moved html.clean to a separate package; trafilatura/justext
# still import the old path. Pulling in lxml_html_clean explicitly keeps
# the clean module reachable.
lxml_html_clean>=0.1.0

# Image processing
opencv-python>=4.8.0

# Location and time
geopy>=2.4.1
timezonefinder>=6.4.1

# /soupystock sparkline (optional — the cog skips the chart gracefully
# if matplotlib is missing, but the rendered embed looks much nicer with it).
matplotlib>=3.8.0

# PyTorch (CUDA 11.8) — install separately:
# pip install torch==2.4.0+cu118 torchvision==0.19.0+cu118 torchaudio==2.4.0+cu118 \
#     --extra-index-url https://download.pytorch.org/whl/cu118

# Local Flux backend (flux_server.py) — install ONLY on the Apple Silicon Mac
# that runs the model; not needed on a bot-only host. Install into the SAME venv
# as the bot (activate .venv first, or use .venv/bin/python -m pip):
#   pip install mflux python-multipart
# Pulls torch/mlx/transformers for Apple Silicon. fastapi, uvicorn and pillow are
# already pulled in above. Verified against mflux 0.17.5.
